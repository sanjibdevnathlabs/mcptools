# Testing dependencies

# Core testing framework
pytest==7.4.3
pytest-asyncio==0.21.1      # For async test support
pytest-cov==4.1.0            # Coverage reporting
pytest-mock==3.12.0          # pytest-style mocking
pytest-timeout==2.2.0        # Prevent hanging tests

# HTTP mocking for API tests
responses==0.24.1            # Mock requests library
httpx>=0.28.1                # For async HTTP testing (compatible with MCP)

# Database testing (for E2E only)
aiomysql==0.2.0             # Already in main requirements, for E2E tests

# Test data generation
faker==20.1.0               # Generate fake test data

# Pytest plugins
pytest-xdist==3.5.0         # Parallel test execution
pytest-sugar==0.9.7         # Better test output
